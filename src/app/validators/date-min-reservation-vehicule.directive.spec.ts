import { DateMinReservationVehiculeDirective } from './date-min-reservation-vehicule.directive';

describe('DateMinReservationVehiculeDirective', () => {
  it('should create an instance', () => {
    const directive = new DateMinReservationVehiculeDirective();
    expect(directive).toBeTruthy();
  });
});
