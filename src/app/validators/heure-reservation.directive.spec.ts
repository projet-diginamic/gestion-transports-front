import { HeureReservationDirective } from './heure-reservation.directive';

describe('HeureReservationDirective', () => {
  it('should create an instance', () => {
    const directive = new HeureReservationDirective();
    expect(directive).toBeTruthy();
  });
});
