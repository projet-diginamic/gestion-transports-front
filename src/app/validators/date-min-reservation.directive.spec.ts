import { DateMinReservationDirective } from './date-min-reservation.directive';

describe('DateMinReservationDirective', () => {
  it('should create an instance', () => {
    const directive = new DateMinReservationDirective();
    expect(directive).toBeTruthy();
  });
});
