import { DateReservationVehiculeDirective } from './date-reservation-vehicule.directive';

describe('DateReservationVehiculeDirective', () => {
  it('should create an instance', () => {
    const directive = new DateReservationVehiculeDirective();
    expect(directive).toBeTruthy();
  });
});
