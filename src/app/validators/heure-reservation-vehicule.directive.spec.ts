import { HeureReservationVehiculeDirective } from './heure-reservation-vehicule.directive';

describe('HeureReservationVehiculeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeureReservationVehiculeDirective();
    expect(directive).toBeTruthy();
  });
});
