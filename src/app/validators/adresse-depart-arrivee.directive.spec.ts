import { AdresseDepartArriveeDirective } from './adresse-depart-arrivee.directive';

describe('AdresseDepartArriveeDirective', () => {
  it('should create an instance', () => {
    const directive = new AdresseDepartArriveeDirective();
    expect(directive).toBeTruthy();
  });
});
